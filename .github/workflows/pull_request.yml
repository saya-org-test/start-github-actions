name: pull-request-workflow pr2
# pull_request 가 발생했을때, 해당 workflow가 실행된다
on:
  pull_request:
    #  명시적으로 타입을 지정해준다
    types: [opened]

jobs:
  #첫번째 job의 이름
  pull-request-job:
    # 어떤 러너를 사용할 것인지 정하는 run s-on
    runs-on: ubuntu-latest
    steps:
      - name: when create pull-request
        run: echo create pull-request
